# Database

The canonical database documentation lives in the repository root: [`../DATABASE.md`](../DATABASE.md).

This pointer exists only so links to `docs/DATABASE.md` keep resolving; do not edit it. Update the root file instead.
