# Requirements

The canonical requirements documentation lives in the repository root: [`../REQUIREMENTS.md`](../REQUIREMENTS.md).

This pointer exists only so links to `docs/REQUIREMENTS.md` keep resolving; do not edit it. Update the root file instead.
