# Architecture

The canonical architecture documentation lives in the repository root: [`../ARCHITECTURE.md`](../ARCHITECTURE.md).

This pointer exists only so links to `docs/ARCHITECTURE.md` keep resolving; do not edit it. Update the root file instead.
